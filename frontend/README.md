# Frontend

React + TypeScript app for chatting with the backend AI service.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

The Vite dev server proxies `/api` requests to the backend at `http://127.0.0.1:8000`.
